# Run a simple ruby script to test the project (pattern borrowed from the Rails project)
script:
 - "ci/travis.rb"

# Compass is Ruby
language: ruby
rvm:
 - 2.1.0

# Install compass
before_install:
 - gem install sass
 - gem install compass
